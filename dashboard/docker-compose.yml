version: '3.8'

services:
  dashboard:
    build:
      context: ..
      dockerfile: dashboard/Dockerfile
    ports:
      - "8501:8501"
    volumes:
      - ../data/production:/app/data/production:ro
    restart: unless-stopped
