# @package _global_
defaults:
  - override /model: ensemble_v1
  - override /features: opponent_adjusted_v1

experiment:
  name: v2_phase4_ensemble
  phase: 4
  description: "Ensemble of Linear and XGBoost (50/50)"
