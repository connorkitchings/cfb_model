# @package _global_
defaults:
  - override /model: linear
  - override /features: recency_weighted_v1

experiment:
  name: v2_phase2_round2_recency
  phase: 2.5
  description: "Testing Recency Weighted (Exponential Decay) Features"

model:
  params:
    alpha: 10.0 # Ridge Regularization
