model.params.n_estimators: range(50, 501)
model.params.learning_rate: interval(0.01, 0.3)
model.params.max_depth: range(3, 11)
model.params.subsample: interval(0.5, 1.0)
model.params.colsample_bytree: interval(0.5, 1.0)
