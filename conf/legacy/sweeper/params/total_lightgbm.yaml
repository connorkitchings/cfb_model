model.params.n_estimators: int(200, 800)
model.params.learning_rate: tag(log, interval(0.01, 0.3))
model.params.max_depth: int(3, 16)
model.params.subsample: interval(0.6, 1.0)
model.params.colsample_bytree: interval(0.5, 1.0)
